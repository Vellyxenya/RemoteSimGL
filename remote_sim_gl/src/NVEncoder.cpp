#include "NVEncoder.hpp"

bool NVEncoder::initialize(int width, int height) { (void)width; (void)height; return true; }
void NVEncoder::encodeFrame(const void* frameData) { (void)frameData; }
void NVEncoder::shutdown() {}
